# ---- 编辑器 / 系统 ----
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# ---- 工作区内部数据 ----
# .workbuddy/ 是 AI 协作会话的过程记忆（设计决策、踩坑记录），
# 属于工作区内部资料、不是项目源码，所以不随仓库发布。
# 其中的结论都已经整理进 README.md。
.workbuddy/

# ---- 测试运行的临时产物 ----
# dom.txt 是 --dump-dom 的原始输出，每次跑自测都会被覆盖，属于中间产物。
# 注意：tools/autotest_report.txt 与 tools/sim_out.txt 是刻意提交的验证证据。
tools/dom*.txt
_*.txt
*.tmp
*.log
~$*
