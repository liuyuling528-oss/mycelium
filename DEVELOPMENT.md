# 开发说明

面向要改这个项目的自己或协作者。玩法介绍见 [README.md](README.md)。

---

## 文件结构

```
index.html                 入口（双击即玩）
style.css                  右侧面板样式

src/config.js              ★ 所有平衡数值都在这里，改它就能调游戏
src/rng.js                 确定性随机 + 噪声（同一颗种子永远是同一张地图）
src/sim.js                 ★ 核心模拟层：地图、资源链、运输、升级、转生（纯逻辑，无渲染）
src/scenes/WorldScene.js   地图渲染与交互（Phaser）
src/ui.js                  右侧 HTML 面板与状态的桥接
src/main.js                启动、存档（localStorage 自动存）

tools/headless_sim.js      ★ 离线平衡模拟（Node 直接跑，不用浏览器）
tools/autotest.js          浏览器端自测钩子（只在 ?autotest=1 时生效）
tools/viewport-probe.html  窄屏探针：在 iframe 里以真机宽度跑自测（见下文）
tools/tune_unlock.js       保留门槛调参工具（扫全曲线 + 解锁/转生顺序检查）
vendor/phaser.min.js       Phaser 3.80.1（已内置，离线可用）
art/gameplay.png           游戏截图
```

**UI 用 HTML/CSS 面板 + Phaser 只渲染地图**：列表、下拉框、滚动条交给浏览器，
比在 canvas 里画文字好得多，代码也短得多。

**不要提交临时产物**：`.gitignore` 已挡住 `tools/_*`（自测 dump、Chrome profile）
和 `.workbuddy/`。跑完测试记得清理，别让 `git add -A` 把 Chrome profile 目录（几百个文件）带进仓库。

---

## 改完一定要验证

这是这个项目最重要的约定。

### 1. 数值平衡 —— 必须先跑这个，再动渲染层

```bash
node tools/headless_sim.js 1800      # 模拟 30 分钟
```

跑 6 种策略 × 5 个种子并给出结论。**通过判据是策略必须真的产生差异**：

```
✅ 策略差异 3.66 倍 —— 决策是有意义的。
✅ 主动点击收益是挂机的 3.20 倍 —— 注意力有价值。
· 拥堵损耗占产出 8.6%
```

后面还挂着四组专项对照（都是「只换一个变量」的口径）：

| 组 | 对照 | 判据 |
|---|---|---|
| C/D | 树周围留不留空位 | 留空位能长出更高级的树 |
| E | 同一经营方式换菌株 | 最强/最弱 ≥ 1.10 倍，且 ≥ 3 个菌株各赢一项 |
| **F** | **离线 8h vs 在线 8h 主动操作** | **在线/离线 ≥ 2.5 倍**（离线必须明显更低）|

**退出码即判据**：任何一组没守住就 `exit 1`，所以可以直接进 CI。

### 模拟时长会改变结论 —— 引用数字前先看口径

`headless_sim` 的第一个参数（默认 1800s）**不是一个无关紧要的档位**，
它直接决定「在线基线」有多高：

| 口径 | 主动/挂机 |
|---|---|
| 600s | **3.20×** |
| 1800s | **1.47×** |

原因是后期「网络自己产」的占比越来越高，主动点击的边际贡献被稀释。
所以**文档里引用的每个数字都要带上时长**，否则换个档位就对不上。
（踩过：写 config 注释时引了 1800s 的 1.47，而 README 那边是 600s 的 3.20，
看起来像自相矛盾，实际是口径不同。）

**为什么这个脚本必须存在**：上一版 Excel 游戏做了 25 项测试全绿，却全是
「不崩、能跑、数值不发散」的工程正确性测试，**没有一项在验证「玩家的决策是否影响结果」**。
结果玩家花不花钱，最终都停在同一关。

教训：**做游戏要先跑「不同策略 → 结果是否有差异」的对照模拟，差异为 0 就是设计失败。**

它还抓出过结构性错误（都是它先发现、我才改的）：

- 节点强化成本增速设成 1.55 时，把养分全砸节点就能碾压其他所有路线（强 3.3 倍）——
  **最优解唯一等于又变回无聊**，改成 1.72 让边际递减
- 吞吐上限一开始设成硬上限，导致 94% 产出被浪费、「扩张反而没收益」
- 菌瘟熄灭逻辑的遍历崩溃（见下文「围死熄灭」）

### 2. 浏览器端 —— 端到端自测

```bash
python -m http.server 8321
chrome --headless=new --virtual-time-budget=60000 \
       --dump-dom "http://127.0.0.1:8321/index.html?autotest=1"
```

当前 **233 项断言**，覆盖引擎启动、真实点击生长、技能冷却、节点强化、
里程碑发奖与防重复发奖、害虫停产与驱除、
**菌瘟蔓延 / 围死熄灭 / 不可净化**、**菌瘟等级规则（绝对免疫线 + 相对往低传）**、
**m9 防火墙（等级门槛 = immuneLevel 的回归护栏）**、
**维护耗水与远端优先降级**（含「降级基准 = 孢子落点」与「距离用 BFS 跳数」两组）、
**自动蔓延的两条规则**（距离上限 / 收益阈值 / 手动不受限 / 存档往返 / 下拉框一致）、
**主干 / 汇流结构**（限额、互斥、产出代价、核心扩容、存档往返、旧档兼容、面板读数一致）、
**树木**（成长档位阈值、古树光环覆盖与不自赏、围拢压制与退化到底、
阈值可达性三条不变量、拆除节点重排 id、存档往返）、
增益区倍率、存档往返、
长时间不发散、自适应布局、
**滚轮 / 拖拽 / 捏合手势**、**转生换图**、**自动蔓延解锁链路**、零 JS 异常。

报告在 dump 出来的 DOM 里的 `<pre id="autotest-report">`，
用 `SUMMARY pass=N fail=M` 和 `ERRORS=` 两行判断结果。

**用 http 起服务，不要用 `file://`。** file:// 下所有脚本都是不透明源，
任何异常都只会报成 `Script error. @?:0` —— 只有文件名没有行号，等于没法排查。
（实测踩过：自测报了一条 Script error，换成 http 后立刻定位到 `ui.js` 某行读了个不存在的属性。）

### 3. 窄屏 —— 用探针，别指望缩窗口

```bash
chrome --headless=new --window-size=1100,900 --virtual-time-budget=80000 \
       --dump-dom "http://127.0.0.1:8321/tools/viewport-probe.html?w=390"
```

**Chrome 在 Windows 上的窗口最小宽度约 500px**，`--window-size=414` 会被静默夹住
（报出来的 `innerWidth` 是 500）；`--force-device-scale-factor` 只改渲染密度、
不改 CSS 视口。所以直接在桌面上测手机宽度是测不到的 —— 媒体查询里的窄屏规则会变成
「写了但从没执行过」。

`tools/viewport-probe.html` 用 iframe 绕开这一点：iframe 有独立的布局视口，
媒体查询看的是它的宽度。

已实测宽度：**1366 / 1024 / 820 / 390 / 320**。

---

## 测试方面踩过的坑

**点击测试的硬要求**：派发事件时要把 `pointer*` 和 `mouse*` **都发一遍**
（`moveTo` / `clickAt` 就是干这个的）。Phaser 在不同环境里挂的监听不一样，
只发 `pointerdown` 时它可能收不到 —— 那样「点了没反应」这类断言会因为
**压根没收到点击**而假通过。

对策：自测里放一个**正对照**（点一个本来就能长的格子，必须长出菌丝）。
正对照失败，后面那些「不生长」的断言全部不可信。

**它抓出过一个肉眼绝对看不出来的 bug**：事件系统原本每次生成都新建 RNG
（`makeRng(seed + n)`），而 **mulberry32 对相近种子的第一个输出高度相关** ——
40 次事件判定全落在同一侧，**害虫永远不出现**，但代码看起来完全正常。
自测的「事件类型有分化」断言（`{"rain":9,"gnat":3,"bloom":8}`）就是为它加的回归防线。

**Phaser 把原生指针事件排队，下一帧才处理** —— 派发事件后同步断言必然失败，必须让出一帧。

**场景必须写 `class extends Phaser.Scene`**。用普通对象字面量传进去，
Phaser 只当它是配置对象，自定义方法不会挂到实例上，表现是「点击地图完全没反应」。

**测试要 arrange 干净**：害虫会累积到上限（之后就再也生不出来）、
里程碑奖励只发一次（抹掉 `unlocked` 只会得到假失败）。两次假失败都是这么来的。

**期望值要在会改变被测状态的调用之前算**。`tick` 里 `checkMilestones` 在 `tickEvents`
之后跑，事后算 `blightSlow` 期望就会拿到另一个值（实测踩过）。

**同一文件的多处编辑必须串行**。曾对 `ui.js` 并行发两个编辑，后写的把先写的盖掉，
文案回退成旧版 —— 而那段文案恰好没有断言覆盖，自测照样全绿。
是靠「线上文件比对」才发现的。**没有断言覆盖的文案，必须用别的方式验证。**

### 自测服务器必须是多线程的（2026-09-22，排查了很久）

`run_autotest.py` 原来用 `socketserver.TCPServer` —— **单线程**，
同一时刻只能处理一个连接。而页面会并发抓 7~8 个脚本
（phaser / config / rng / sim / WorldScene / ui / main / autotest），
多出来的请求被搁置，Chrome 把它们判为失败。

症状极具误导性，值得完整记下来：

- 某个 `<script src>` 触发一个**空的 error 事件**（`message` 和 `filename` 全是空），
  该脚本**从未执行** → `UI` 未定义 → `main.js` 第 20 行抛 `UI is not defined`
  → `MYC.game` 永远建不起来 → autotest 报 `FAIL 游戏未能在 8 秒内启动`
- 报告里 `SUMMARY pass=0 fail=1`，**看起来就像整个游戏启动失败**
- 而**源代码完全没问题**：`node --check` 全过、
  `vm` 里跑 ui.js 能正常定义 `UI`、孤立的最小页面（同样这几个 script）也正常
- 而且它是**间歇性的**：同一份没改过的页面连跑 8 次，7 次正常 1 次失败

排查过程中有两个陷阱：

1. **一开始误判成「ui.js 有问题」**，甚至写了个二分脚本去剥离 DOM 区块，
   结论忽左忽右 —— 因为失败本来就是随机的，任何二分都在测噪声。
   **先确认失败是否可复现，再去做二分**，否则是在追影子。
2. **`--dump-dom` 抓到的 DOM 长度是个很好的信号**：
   失败时 DOM ≈ 6400~13000（面板没渲染），成功时 ≈ 16500（面板填充了）。

解法 `socketserver.ThreadingTCPServer` + `daemon_threads = True`。
另外保留了一个「拿不到报告就重试最多 4 次」的兜底，
但**真正的修复是线程化** —— 重试只是兜底，不要用它掩盖问题。

### 自测必须自己重置起点，否则结果不可复现

自测会玩一整局（长满网络、打通全部里程碑、转生十来次），结束时把状态留在
`localStorage` 里。**在同一个浏览器 profile 上重跑，读到的就是那个「终局存档」。**
于是「开局该是锁定状态」「地图档位该是 0」这类断言必然失败 ——
看起来完全像回归，实际只是上一次自测的残留。

更麻烦的是它**会搬家**：修好一条，失败就跑到下一条去（实测：修完 4 条后
失败变成「养分在产出」和「网络有最大离核距离」）。因为整条链的起点是脏的，
任何依赖「网络该是自己长出来的」断言都可能中招。

**解法：把重置作为第 0 步，在断言开始前清干净。**

```js
localStorage.removeItem(UI.KEY); localStorage.removeItem(UI.SEEDKEY);
var s = Sim.newGame(SEED, {}, {});
game.state = s; UI.rebind(s);              // 必须 rebind —— main.js 已把旧引用交给了 UI
scene.onMapChanged();                       // 让场景缓存与镜头跟着重置
```

**不能只删 localStorage**：`main.js` 已经把旧 state 的引用交给了 UI 与场景，
删掉存储不会换掉内存里的对象。

**重置后要显式驱动一次 `UI.update(0, game)`**：`rebind` 只强制刷新自动蔓延开关
一处，锁定状态的完整刷新在 `update` 的解锁检查分支里，而那个分支按
`lastAgLocked` 做增量 —— 重置时它可能恰好等于目标值而不重刷。

> 教训：**「测试失败」要分清是回归还是污染**。判据是「它是否依赖运行顺序」——
> 如果失败的位置会随修改而搬家，那先查起点干不干净，别急着改产品代码。
> 这次真实的产品 bug（配额量化）就藏在一堆污染造成的假失败里，
> 差点被当成同一个问题糊过去。

`?demo=秒数` 可以快速养大网络，方便截图和目视检查。

---

## 关键实现说明

### 手势与镜头

| 操作 | 效果 |
|---|---|
| 滚轮 | 以指针位置为锚点缩放（指哪放大哪） |
| 按住拖动（单指 / 鼠标） | 平移视野 |
| 双指捏合 | 缩放 + 双指平移 |
| 右上角 ⟲ | 回到自动取景 |

- **点击与拖拽的判定放在「抬起」时**，位移超过 6px 就算拖拽。
  如果按下就执行，拖动一下就会顺手长出一格。
- **手动缩放/平移会停掉自动取景** —— 否则每帧的自动缓动会把刚拖到的位置立刻顶回去，
  用起来像镜头在跟你抢。
- 缩放范围：不小于「整张地图刚好装下」×0.85，不大于每格约 62px；平移中心始终夹在世界内，**拖不丢**。
- 捏合期间按下的手指，抬起时**不算点击**。这里不能用「时间窗」做冷却：
  冷却走的是游戏时间，rAF 一停（切后台、headless 帧稀疏）时间就不走了，
  窗口永远不过期 —— 实测踩过，已改成按指针登记。

### 自适应取景

- **桌面（宽）**：左侧地图铺满剩余空间，右侧面板独立滚动，地图钉在视口里
- **窄屏**：单列，地图 sticky 在顶部；高度用 `svh` 而不是 `vh`，地址栏收放时地图不跳
- 地图自动取景：视野对准「已探明区域 + 一点余量」，网络长大镜头缓缓拉开。
  两个硬约束：不大于每格 46px（否则开局几个格子会被放成马赛克），
  不小于「整张地图刚好装下」（**绝不裁掉网络的任何一部分**）
- 触摸 1 格容差：手指点偏一格自动落到旁边可操作的格子；鼠标不启用

### 转生换图

- 尺寸阶梯：边长 = 基础 34×20 × `min(2, 1 + 0.12 × 地图档位)`，最多 68×40（面积 4 倍）
- **驱动量是 `state.mapTier`，不再是转生次数** —— 地图现在只在玩家
  选了「扩大地图」卡时才变大（见下面「转生三选一」一节）
- 新种子由旧地图**确定性推出**（不能用 `Math.random` —— 平衡对照模拟靠
  「同种子 → 同地图序列」比较策略，一随机每次跑出来的结果就不可比了）
- 地形特征数量按**面积**缩放，密度不变 —— 大地图不会变成空地
- 经济封顶在面积 4 倍：孢子收入正比于节点数，地图无上限就等于经济无上限

顺带修过一个老 bug：**读档从来没把探索记录应用回网格** ——
读档后已探明区域整个消失，只剩核心周围一圈。现在 `deserialize` 会正确恢复。

**旧存档迁移**：`mapTier` 是后加的字段。老存档没有它，
`deserialize` 会按 `min(mapMaxTier, prestiges)` 反推 ——
口径是「老玩家当年每次转生都白拿一次变大」，所以按转生次数补回档位，
地图不会被打回原形。**不回缩**是这条迁移唯一的硬要求。

### 菌瘟生命周期（围死熄灭）

- 每处菌瘟独立存亡（per-event，不是全局疫情）
- 每 `spreadInterval`（21s）尝试传播一次
- **池子非空 → `failStreak = 0`**：有活路就一直活着。
  关键语义：**抽签没中不算被围死** —— 它可以传，只是这个周期运气差
- **池子为空（被围死）→ `failStreak++`**，达到 `failLimit`（2）→ splice 事件、
  节点恢复健康、浮字「熄灭」
- 等级规则：绝对免疫 + 相对比较，两条叠加
  - **绝对**：`t.level >= immuneLevel`（7）→ 完全免疫，任何瘟都传不进
  - **相对**：否则只传 `t.level ≤ min(nd.level, maxLevel)` 的邻居
  - `blightableNodeIds`（滋生池）过滤 `level < immuneLevel`
- **免疫线 = m9 防火墙线（同一个数字）**：三处（`immuneLevel`、
  `spreadBlights` 的传染判定、m9 的等级门槛）必须引用同一个
  `BLIGHT.immuneLevel`，不许各自硬编码。详见 config.js 里的「不变量」注释
- `removeBlight` 已降级为**内部接口**（无奖励/无计数），仅供测试编排与清场

**踩过的真 bug**：`tickEvents` 原先是直接遍历 `state.events`，
而 `spreadBlights` 熄灭菌瘟时会 splice 原数组 —— 一晚熄灭多处就让 `events[i]`
变成 `undefined` 崩掉整个模拟（平衡模拟实测抓到）。旧版自愈只在当前索引 splice
所以从未暴露。

**修复**：快照遍历（`events.slice()`）+ 到期事件按身份统一移除（快照索引不可信）。
**任何「回调里会删数组元素」的遍历都要先快照。**

---

## 设计上刻意避免的两个陷阱

| 陷阱 | 后果 | 这里的处理 |
|---|---|---|
| 敌人/难度与玩家同步成长 | 相对强度恒定，玩家永远感觉不到变强 | 资源靠「距离损耗 + 拥堵」而非难度曲线；成长感来自网络拓扑 |
| 吞吐上限设死 | 扩张反而没收益，与增量游戏根本冲突 | 改成**软上限**（超载仍过 30%），并用水/货物分带宽（水不受限） |

第二条是实测发现的：早期版本让 98 个高产水脉把带宽全占满，养分和孢子运不回来，
**扩张反而把自己饿死**，94% 的产出被浪费。现在水作为生长介质自由流动，
拥堵只发生在真正的货物上。

---

## 数值决策记录

### 自动蔓延门槛 = 250

用平衡模拟扫完整条曲线（0/60/…/500，1 次/秒点击）：

| 门槛 | 解锁耗时 | 30 分钟转生数 | 终局菌丝 |
|---|---|---|---|
| 0（只看基质） | ~3 分钟 | 5.0 | 410 格 |
| 120 | ~4 分钟 | 5.0 | 400 格 |
| 200 | ~5 分钟 | 5.0 | 386 格 |
| **250** | **~7 分钟** | **5.0** | **337 格** |
| 300 | ~12 分钟 | 5.0 | 161 格 |
| 400 | ~20 分钟 | **4.3（开始伤进度）** | 587 格 |
| 500 | ~25 分钟，部分种子解不了 | 4.0 | 449 格 |

200 → 250 的长期代价为零，300 开始曲线陡增（解锁时间翻倍、种子方差拉大），
所以取 250：比直觉的"200"稍紧一点，但不伤任何后续进度。

### 节点强化成本增速 = 1.72

设成 1.55 时，把养分全砸节点就能碾压其他所有路线（强 3.3 倍）。
**最优解唯一等于又变回无聊** —— 改成 1.72 让它有明确的边际递减，
与全局升级形成竞争。

### 菌瘟参数

| 参数 | 值 | 说明 |
|---|---|---|
| `minNodes` | 32 | 网络到这个规模才可能出现 |
| `maxCount` | 8 | 同时最多几处，永远不会掏空网络 |
| `maxLevel` | 7 | 自动滋生的最高宿主等级 |
| `immuneLevel` | 7 | **免疫线**：Lv7+ 不被滋生、也不被任何瘟传染 |
| `spreadInterval` | 21 | 每处每 21 秒尝试传播一次 |
| `spreadChance` | 0.55 | 扩散成功率（随转生次数小幅上调，上限 0.85） |
| `failLimit` | 2 | 连续 2 个周期传不出去 → 熄灭 |
| `firewallNodes` | 3 | m9「防火墙」要求几个达到免疫线的节点 |

### 免疫线 = m9 防火墙线（不许分家）

`immuneLevel`（谁绝对免疫）和 m9 的等级门槛本来是两个参数，早期确实分开了：
`immuneLevel = 7` 但防火墙要求 **Lv8+**。结果是规则分裂成两套 —— 玩家练到
Lv7 已经免疫了，却还要多练一级才算「防火墙」，**这条里程碑在惩罚玩家按免疫线走**。

现在三处引用同一个 `BLIGHT.immuneLevel`，语义自洽：**练到免疫线 = 免疫 =
构成防火墙**。自测里有一条回归护栏专门盯这件事（参数分家就炸）。

**`firewallNodes = 3` 不是平衡旋钮。** 别按「养不养得起」去调它 ——
能凑出 3 个 Lv7 免疫节点就已经是大后期，那点维持费在后期水收入面前是账目噪声。
（实测：7 个 Lv7 要 700/s，而正常玩家后期水产量中位仅 **416/s** ——
如果按「养得起」来定这个数，防火墙会变成永远刷不过去的关卡。
这个账算过一遍就知道该问的不是「养得起吗」，而是「几个算一组」。）
它只需要满足一件事：**量要足够少，少到能叫「一组」**。

### 维护耗水：为什么会有它、为什么是平方曲线

**起因**：水的唯一支出只有 `growCost`（生长），而地图 34×20=680 格、
可长格约 641 —— 实测 **5 分钟就铺满**，之后水产量 500~770/s 全部沉在池子里，
20 分钟涨到 **82 万**（`tools/probe_water*.js`）。水彻底失去意义。

**关键判断**：这不是「后期」问题，是**地图容量**问题 —— 从铺满那一刻就开始了。
而且水从来不是瓶颈（后期产量是消耗能力的 100 倍量级），只是在铺满前被掩盖了。

**设计选择**：

| 决定 | 理由 |
|---|---|
| 维持费**只挂等级**，不挂节点数 | 挂节点数会变成「扩张惩罚」，和「扩张永远有收益」的基本盘冲突。只挂等级 → 维持费正比于**投入的深度**。<br>**Lv0 维持费为 0 是刻意的**：卡在 0 级就是没投资，不该收费。 |
| **超线性（等级²）**，不是线性 | 见下节 —— 线性会让「摊薄永远最优」，和机制初衷相反 |
| 缺水时**只降级、不摧毁节点** | 守住「绝不永久损失」底线。代价不在水里，而在产出/吞吐掉回上一级 |
| **远端先降**（`dist` 降序，同距先降高等级） | 把「位置」从「只是运输损耗」提升成「占着就有成本」—— 想维持远处的富矿，就得沿路多留水 |
| 缓冲线 `bufferSec = 40` | 不给缓冲会「一进水就疯狂降级」，等级来回跳。缓冲让玩家有时间反应 |

**「距离」的基准点 = 孢子落点**（`state.core`，即节点 #0 所在格）。
不是地图几何中心 —— 34×20 时 `m.w>>1` = 17，而几何中心落在格 (16,9)/(17,10) 的
交界线上，用几何中心会引入恒定的半格偏移。玩家说的「离中心远」指的是**离落点**，
降级顺序也必须以它为准。`tools/probe_downgrade_order.js` 与自测里的
「降级基准」断言组守着这条。

**用的距离是 BFS 跳数（沿菌丝网络），不是欧氏直线。**
绕路生长时两者会给出相反的顺序：L 形网络里一个节点可能欧氏 6.1 格、
BFS 却是 7 跳。用 BFS 才对，因为「维持一个远端节点」的真实代价是
**它到核心要经过多少跳**（每一跳都有吞吐损耗），而不是直线有多近。
自测里有一条断言专门找「欧氏更远但 BFS 更近」的节点对，确认排序信的是 BFS。

### 为什么是平方，不是线性

维持费曲线是 `等级 ^ exponent × costPerLevel`，现在 `exponent = 2`、
`costPerLevel = 100/49 ≈ 2.0408`（让 **Lv7 恰好 = 100/s** 这个整数锚点）。

**线性（`level × K`）有个致命后果**：维持费只取决于**等级总和** ——
「10 个 Lv10」和「100 个 Lv1」成本完全相同（都是 100 级）。但摊薄能抗降级
（一次只掉 1 级、且分散），深耕却是「一掉掉一片」，于是**理性玩家必然放弃深耕**，
最优解永远是摊平。这跟机制初衷（让「养哪些节点」成为取舍）正好相反。

对照（`tools/probe_maint_shape.js`，等级总和都是 100）：

| 曲线 | 集中/摊薄 | 评价 |
|---|---|---|
| 线性 | 1.00× | **摊薄零代价，深耕被惩罚** —— 不可用 |
| `^1.5` | 3.16× | 但 Lv10 只剩 2 个可养，满级几乎不可达 |
| **平方（采用）** | **10.0×** | 深耕有真代价，满级仍可达 |
| 指数 | 13.2× | 只养得起 1 个 Lv10，太硬 |

另一层理由：**升级成本本身就是指数**（`26 × 1.72^level`）。升 Lv10 要付
3,426 养分，而旧的线性维持费（`level × 6`）只比 Lv1 多 54/s ——
高等级区域等于几乎不收费，两条曲线完全脱节。平方后 Lv10 = 204/s，
和「练它花了 3,426 养分」的量级匹配上了。

**可维持承载力**（后期水产量 ≈ 554/s 口径）：

| 布局 | 可养数量 | 等级总和 |
|---|---|---|
| 全 Lv1 | 271 个 | 271 |
| 全 Lv3 | 30 个 | 90 |
| 全 Lv7 | 5 个 | 35 |
| 全 Lv10 | 2 个 | 20 |

即：**摊薄铺得开（Lv1 几乎免费），想练高就得挑地方** ——
自然产生「近核富矿练高、外围垫低等级」的梯田式布局，
而不是要么全摊平、要么全深耕的两极。

> **口径警告**：这些数字对「后期水产量」极为敏感。实测三种口径差 6 倍 ——
> 零升级挂机 **69.5/s**、正常玩家（买升级）**415.8/s**、深耕玩家 322/s
> （`tools/probe_water_rate.js`）。定平衡**必须用「正常玩家」口径**：
> 只测挂机会严重低估承载量。曾因为漏了买升级而把水产量算成 69/s，
> 得出「免疫线不可达」的错误结论。

### k 扫描（`tools/probe_maintain.js`，线性时代的记录）

| k | 终局水量 | 降级 | 平均等级 |
|---|---|---|---|
| 0 | 147 万 | 0 | 5.11 |
| 0.25 | 69 万 | **0** | 5.11 |
| 0.40 | 23 万 | **0** | 5.11 |
| 0.70 | 0 | 3327 次 | 5.11 → 3.67 |
| 1.00 | 0.2 万 | 4577 次 | 5.11 → 2.34 |
| **6.0（曾采用，已废弃）** | — | — | 见下方「配额才是瓶颈」 |

**0.70 的悬崖**（0.40 零降级 → 0.70 就掉 3327 次）说明「靠 k 去贴降级临界点」
是脆的：落在哪完全取决于玩家的收入水平，收入差一点体验就完全不同。

**另一条关键性质**（也是断言守着的）：不练节点的纯扩张流在**任何 k 下都零降级**——
维持费精准打击深耕流，不误伤扩张流。

### 配额才是真瓶颈，不是 k（重大修正）

上表的读法**曾经是错的**。k 从 1.0 涨到 12.0 时，等级总和从 206 掉到 16，
但累计降级次数几乎不变（4798 / 4673 / 4493）—— 如果 k 是瓶颈，
降级次数应该跟着涨。它不变，说明真正的约束在别处：**降级配额**。

早期实现是「每秒固定降 N 级」的硬配额，于是……

- **k=0.25 时机制等于不存在**。这是最初采用的值，为了「温和」。实测下来
  可维持等级总和 2214 级，而玩家 20 分钟养分预算只够练 143 级 ——
  维持费永远追不上，等于装饰。用户直接否掉：「一定要有负反馈，
  全部节点都满级绝对不应该。」
- **k 提高后压死深耕流**。平均等级 3.32 → 0.60，累计降级 519 → 4787，
  表现成「一练就掉」的自动循环。因为固定配额下，玩家练 1 级立刻被砍掉，
  等级永远堆不起来 —— 提高 k 完全无效。

**修法有两处，都很关键：**

**① 降级速度与缺水程度成正比**，而不是固定每秒 N 级：

```
scale = 0.15 + deficit² × 2.85      // deficit: 0（刚破线）→ 1（见底）
quota = downgradePerSec × scale × dt
```

刚好破线时几乎察觉不到，真的见底就干脆利落地收缩。**k 这才成为一个有意义的旋钮。**

**② 配额不足 1 级时必须累加，不能白扔**（`state.maintainQuota`）。
浏览器 60fps 下单帧 `dt ≈ 0.0167`，轻度缺水时 `quota ≈ 0.0075 级/帧`，
恒定小于 1。若直接丢弃，「碰到缓冲线就开始缓慢收缩」就成了空话 ——
实际表现是彻底卡住不动。累加到 1 级再降，等价于平均每秒降 `scale × perSec` 级。

**连带修掉一个更隐蔽的 bug**：循环条件写成 `downgraded < quota` 时，
只要 `quota > 0` 就至少降 1 级 —— 降级速度在低端被量化成固定每秒 1 级，
缩放完全失效，玩家看到的是「刚开始缺水就猛地掉级」。
改成预算制 `downgraded + 1 <= Math.floor(quota)` 后，
`quota ∈ [0,1)` 一帧不降、`[1,2)` 最多降 1 级。
（还有一处浮点坑：`quota` 精确等于 1.0 时旧写法会降 2 级，
因为它被算成 `1.0000000000000002`，撑过了第二次比较。）

**可维持等级总和的判据**（用来定曲线参数，比「看心情」可靠）：
玩家养分预算能练出的总级数 **vs** 水能养住的级数。
玩家 20 分钟累计养分 48,736 → 约能练出 **143 级**；
平方曲线 + `costPerLevel = 100/49` 下，水只养得住 **35~90 级**
（取决于把等级堆在少数节点还是摊开）→ 玩家必须舍弃一大半，
「养哪些节点」才成为真正的取舍。

> **顺带纠正一个曾算错的判断**：曾称「练满一网络只需约 23 万养分」，
> 那是**忘了乘节点数**。练满 641 节点到 Lv10 需 **522 万**，
> 而 20 分钟累计养分仅 48,736 —— 差 100 倍。
> 所以「全网络满级」当前根本不可能发生，真正的天花板是**养分**，不是水。

### 维护费引入后重跑平衡（1800s / 5 种子）

| 指标 | 结果 | 判据 |
|---|---|---|
| 策略差异（最强/最弱） | **3.30 ×** | >1.5 ✅ |
| 主动点击 / 纯挂机 | **1.87 ×** | >1.15 ✅ |
| 拥堵损耗占产出 | 6.8% | 低位 ✅ |

**深耕流明显垫底**（转生 2.20，最高等级仅 5.2；扩张流 6.00、Lv0）。
用户明确表态：**这不值得为此调整平衡** ——
「0 级不需要维持费是对的，后续不平衡再改」。理由站得住：
扩张流（`终局菌丝 844 格`、累计养分 153 万）本质是「铺满地图」的主流派，
深耕流是「高难选项」。**扩张流领先是当前有意接受的状态**，
只有当它变成「怎么玩都追不上」才值得动。

**另一条要记住的性质**（断言守着）：不练节点的纯扩张流在**任何曲线下都零降级**——
维持费精准打击深耕流，不误伤扩张流。

### 主干菌丝 / 汇流节点：为什么不是「给沿线节点加吞吐」

规划评估文档给的方案是「允许把节点标为主干，主干吞吐 ×N，但主干数量有上限」。
**照原样实现后跑平衡模拟，收益是 ×1.000 —— 完全无效。**
文档自己的判据是「差异为 0 就说明设计失败」，所以必须先查清原因再动手，而不是调数值。

原因在逐距离货流剖面里（270 节点全 Lv10，见下表）：

| 距离 | 节点数 | 总货流 | 单节点吞吐 | 段负载率 |
|---|---|---|---|---|
| **0（核心）** | 1 | 152.6/s | **110** | **138.7%** |
| 1 | 4 | 152.6/s | 392 | 9.7% |
| 2 | 8 | 152.4/s | 140 | 13.6% |
| ≥3 | … | 递减 | 140~392 | **全部 < 14%** |

**全网络只有一个瓶颈：核心。** 沿途菌丝从来没堵过。
原因是 `coreCapacity: 110` 是**裸值**，核心没有等级可以练，
而普通节点有 `capPerLevel 0.18` 的等级加成 —— 于是网络越大，核心越堵，
而这件事跟「线路选得好不好」毫无关系。

所以主干的正确定位是：**它不是给沿途扩容，而是决定核心能吃进多少货。**
`coreCapPerTrunk: 0.22` → 满配 5 根让核心 = `110 × (1+1.1) = 231`。

改完之后的实测（`tools/headless_sim.js`，1800s / 5 种子）：

| 主干标在 | 核心 cap | 拥堵损耗 | 转生 | 基因 | 综合分 |
|---|---|---|---|---|---|
| 不标 | 110 | 111810 | 6.00 | 96.2 | 30961 |
| **贫矿（水脉）** | 231 | **16200** | 3.80 | **86.2** | **28360** |
| 富矿（反例） | 231 | 27136 | 4.00 | 48.2 | 25493 |

- 主干把拥堵损耗从 111810 降到 16200（**-85%**）—— 机制真的作用在瓶颈上了。
- **「标在哪里」有 11% 的收益差**（28360 vs 25493）：
  两个策略只有选址不同，贫矿胜出。因为主干节点自身产出 ×0.55，
  拿腐木/树根当主动脉等于白白损失产地，拿本来只产水的水脉则几乎无代价。
- `coreCapPerTrunk` 不能设 0.30：实测那样标 3 格就解除瓶颈，
  第 4/5 格收益归零，「凑满配额」的动机消失。0.22 让每格都有可感知的边际收益。

**数值上的两条硬约束**（断言守着，别改）：
1. **主干必须有硬上限**（`maxTrunk: 5`）。没有上限时最优解永远是全标主干，
   机制就退化成一次性的全局倍率 —— 这正是评估文档警告的「又一个倍率升级」。
2. **主干必须有产出代价**（`trunkYieldMul: 0.55`）。没有代价它就是纯赚，
   「标在哪里」不再是一个决策。

**为什么汇流要跟主干一起做**：二者互斥且共享「通道 vs 产地」的同一个交换。
汇流解决的是「多条支流汇入同一处」的局部瓶颈，`feederAdjMul: 1.35`
让「把支流停在汇流点旁边」有位置上的意义。单独做没有意义 ——
评估文档也是这么说的（「必须和主干一起做才有意义」）。

### 自动蔓延的两条规则：为什么是「上限」而不是「权重」

规划评估文档明确要求「不要过度设计」，只加两个过滤条件，不做规则引擎。
两个规则分别对应两条真实抱怨：自动蔓延往远处跑把水耗光、以及连贫地。

**关键取舍：用硬上限，不用调权重。** 距离在 `scoreCandidate` 里本来就有惩罚项
（`- c.dist * distWeight`），但它是**软**的 —— 远处一块富矿的加分足以盖过它，
玩家只能眼看着水被拉走却无处可拦。硬上限让玩家能确定性地说「别超过 N 格」。

**规则只作用于自动蔓延，手动点击永远不受限。** 这是手感底线：
玩家想现在就连哪一格，不该被自己的自动化配置挡住。
用 `bestCandidate(state, policy, autoOnly)` 的第三个参数区分两条路径，
测试里有一条专门的断言守着（「手动点击不受规则限制」）。

两条规则存在 `state`（`ruleMaxDist` / `ruleMinYield`）而不是配置常量 ——
它们是玩家的操作偏好，要进存档、跨转生、跨会话保留。
`rebind` 也必须按新 state 落值（与自动蔓延开关同一个坑，见上文）。

实测（`tools/autotest.js` 断言值）：
- 上限 6 → `maxDist` 稳定停在 6，不再超出
- 阈值 1.0 → 新增 25 格里低于阈值的 0 格
- 手动点击 → 能连上产出低于阈值的 `litter`（正确放行）

### 树木：阈值必须对着**实测分布的个体下沿**定，不是中位数

规划评估文档对树只提了一个硬要求：
**「不要让树变成又一个资源格 …… 树要能影响周围（比如古树周围的菌丝产出 +X），
那样它才是空间决策，否则只是更肥的格子。」**

所以树的设计不是「树根产出更高」，而是三件事：
1. 树随连接时长成长（幼苗 0.6× → 成年 1.0× → 古树 1.6×，`stageAt: [0,60,180]`）；
2. **古树给周围 2 格内的其它菌丝 ×1.25**（`auraYieldMul`，不叠加）——这是「影响周围」的落点；
3. 树周围菌丝太密会**压住成长**（`crowdSoftCap` / `crowdPenalty`）——这是唯一的负反馈。

第 3 条不是装饰：**没有它，最优解永远是「把所有树都围满」**，
树就退化成「一块产出高一点的壤土」，正好落进评估文档警告的那个坑。

#### 先测出参照系：两张实测分布表

这一块的数值前后改了**五轮**，每一轮都是被实测打回来的。
第一个教训是：**在拍任何阈值之前，先把「自然密度」测出来。**
两张探针表（都是 5 个种子）：

**参照系 1 · 正常铺开的网络**（②扩张流 的经营方式，600/900/1200/1800 秒，
共 **560 棵树**）：

| 统计量 | min | p10 | p25 | p50 | p75 | p90 | max |
|---|---|---|---|---|---|---|---|
| 树的 2 格邻域 `crowd` | 5 | 16 | 17 | **19** | 20 | 20 | 20 |

**参照系 2 · 密铺到极限**（铺到 630~650 格，共 **140 棵树**）：

| 统计量 | min | p10 | p25 | p50 | p75 | p90 | max |
|---|---|---|---|---|---|---|---|
| `crowd` | 5 | 16 | 17 | 19 | 20 | 20 | 20 |

直方图：`{5:1, 6:1, 8:1, 10:2, 11:2, 12:3, 13:2, 15:2, 16:4, 17:25, 18:12, 19:16, 20:69}`

从这两张表读出三条硬事实：
1. **中位数 19、众数 20**，20 就是 `ringRadius=2` 圆形邻域的几何上限 ——
   地形允许时玩家自然会把树围满；
2. 只有 ~7% 的树 `crowd < 13`，而且几乎全是地图边缘的，不是玩家刻意留的；
3. **约 9% 的树被岩石/邻树卡在 17 以下** —— 密度只能到 17，再也上不去。
   这一条是后面所有麻烦的根源。

#### 踩过的坑 1：把阈值定在自然密度**上方** → 机制作废

第一版把 softCap 拍成 6，理由是「一棵树旁边围 6 格算合理」。
结果跑平衡模拟，**所有策略的树都停在 0.00~0.09 档**，
「留空位」和「围满」只差 0.05 档 —— 直接触发评估文档的判据「差异为 0 就说明设计失败」。

根因：我拍的是「几何直觉」，而真实网络**不是几何图形**。
按上表，`crowd` 的自然值就有 17~19，cap=6 意味着
**任何被接进网络的树都永远超标**，永远停在幼苗。
玩家看到的「养树」是一条**不存在的能力**，而不是一条难走的路。

#### 踩过的坑 2：把阈值定在自然密度**下方太多** → 机制变成惩罚

修完 cap 就改成 13（「自然通路 + 一条支路」的余量）。这次反过来错了：
对上表，cap=13 意味着 **93% 的树永久停滞**。
玩家只要正常铺开，树就全废 —— 这不是「空间决策」，
是「不按设计者的方式玩就挨罚」，比 cap=6 更隐蔽但一样是设计失败。

**两次错误的共同病因**：阈值和自然密度之间没有「坡度」。
- cap 高于自然密度 → 机制不触发；
- cap 远低于自然密度 → 机制永远触发。
- **只有落在分布之内，才有「一部分树舒服、一部分树被压」的可选空间。**

#### 踩过的坑 3（最阴的一次）：阈值卡在**中位数**上 → 局部个体免疫

第三版取 `softCap=17 / penalty=1/2` → stall 线 = **19**，正好压在 p50 上。
全局统计看着完美：典型树正好停在停滞线上，「不特别照顾」= 不再长但也不烂掉，
想成古树就腾一格 —— 听起来很理想。

自测直接打回：受试的树密铺 **653 格**、整张图铺满，`crowd` **只能到 18**。
查参照系 2 的直方图就明白了 —— **约 9% 的树被岩石/邻树卡在 17 以下**，
stall 线 19 对它们是**永远够不到的**，于是「围满会退化」这条规则
对这些树**整条失效**。

这是最危险的一版：全局均值/中位数完全正常、单看数字毫无破绽，
只有拿**具体某一个个体**去跑才会暴露。
**教训：阈值要对着分布的个体下沿定，不能对着中位数或均值。
中位数漂亮，不代表每个个体都够得到。**

#### 最终取值：`softCap = 16` / `penalty = 1/2` → stall 线 18

| 参数 | 值 | 依据 |
|---|---|---|
| `crowdSoftCap` | **16** | 低于实测 p25（17）。什么都不刻意做时，约 1/3 的树自然在 cap 内、正常长大；想多养古树就得腾空位，每棵的代价是少铺 2~3 格 |
| `crowdPenalty` | **1/2** | `stall 线 = 16 + 2 = 18`。18 同时满足两个硬约束 ↓ |
| `decayPerSec` | 0.25 | 倒退**下限**（不是上限），见坑 5 |

stall 线取 18 的两个约束：
- **上界安全**：实测 86% 的树能到 18 以上（只有 ~9% 卡在 17 以下），
  所以「围满 → 退化」对绝大多数树**真的会发生**；
- **下界有坡度**：18 高于 p25（17），典型树仍会依次经历
  「满速 → 减速 → 停住 → 倒退」四段，而不是一上来就烂掉。

最终曲线（`softCap=16, penalty=1/2`）：

| crowd | rate | 表现 | 实测占比 |
|---|---|---|---|
| ≤16 | 1.00 | 满速成长 | ~p10 |
| 17 | 0.50 | 还能长，只是慢 | p25 |
| 18 | 0.00 | **停滞线** | p50 附近 |
| 19 | -0.50 | 倒退 | |
| 20 | -1.00 | 倒退明显 | **35%** 的树能到 |

四段全部落在真实可达范围内，且每一格都有可测量的差别。

#### 踩过的坑 4：光填「树周围的 2 格环」根本造不出被围住的局面

自测里要构造「围满」，第一版是把树周围 2 格环里能铺的都铺上。
实测只到 `crowd=13~18`，怎么都越不过 stall 线。两个原因：
1. 上一测试步「无干扰成长」为了排除干扰**故意把树周围清空过**，
   而 `growAt` 要求相邻 —— 环上的格子未必接得上网络；
2. 真实地图上那棵树周围本来就有岩石/别的树，环站不满。

**修正**：不再「只填环」，而是让 `bestCandidate` 把**整张网络铺密**
（这正是真实玩家的铺满行为），然后**密铺之后再按「谁被围得最狠」挑受试树**。
这样受试者一定能越过 stall 线，「围满会退化」这句话才有正确的实验条件。

顺带一个后见之明：如果一开始就按「先铺密、再挑树」来写，
坑 2 和坑 3 里那些「阈值到底该定多少」的纠结会**自己浮出来** ——
因为探针会直接告诉你「玩家正常玩时树有多挤」和「铺到极限时能挤到多少」。
**先建立观测，再调参数。**

#### 踩过的坑 5：`Math.min` 写成了上限，退速被压到 0.25/秒

倒退那一行第一版是 `Math.min(-rate, decayPerSec)`。
本意是「退速别太猛」，但 `min` 取的是**较小值**：
`rate = -3.9` 时退速被压成 `0.25/秒`，古树被围满后 12 分钟才退到档位线，
探针里跑了 240 秒只从 180 退到 120 —— **正好卡在档位边界上，
看起来像「树被围住后完全没反应」**，和「围满会让树退化」的承诺相反。

想要的语义是「至少退 `decayPerSec`，超得多就退得快」，那是对 `|rate|` 取
**`max`** —— `decayPerSec` 是**下限**不是上限。
改成 `Math.max(-rate, decayPerSec)` 后，实测 180 → 0 用 185 秒
（`crowd=20` 时 `-rate = 1.0 > 0.25`，按 1.0/秒退），三档逐级回退。

**教训：`Math.min` / `Math.max` 想表达「上限」还是「下限」，
写的时候必须用一句话把语义念出来**（「至少」「至多」），
否则单看代码很难发现反了 —— 而且反了之后**数值仍然是个合理数字**，
不会报错、不会 NaN，只会静默地让机制失效。

**推论：负反馈的验收必须量「最终落到哪一档」，不能量「此刻速率是正是负」。**
上面 0.20 那版 rate 恰好 = 0.000，看着像「压住了」，
但树的成长度一动不动 —— 用「速率」验收会误判通过。
所以自测里断言的是「成长从 180 一路回到 0 且档位退回幼苗」（实测 185 秒跑完）。

#### 踩过的坑 6：光秃秃的地图上「养树」根本走不通

树的兑现全在「长大」，古树要 180 秒。
但地图生成里树根是**成团撒在随机位置**的（`rootBlobs: 9`），
实测 5 个种子里，核心感知圈内**从来没有树根**（开局候选格只有 `litter`）。
远处（dist 15+）的树根要玩家先修一条长路才够得着 ——
等路修通，这局已经转生了，树根本来不及长成。

**修正：在核心外圈 3~8 格补 3 块树根**（`GEN.rootBlobsNear`，见 `placeRootsNearCore`）。
刻意**不放**在感知圈内 —— 那样开局白送一棵树，缺了「把它接进网络」这一步。
要的是「看得见、走两步够得着」。

#### 配套：拆除模式

「克制着别铺满」如果没法反悔，就只是一句空话 —— 玩家铺错了回不了头。
所以加了 `Sim.removeNode` + 面板上的「拆除」模式（第 4 个结构模式按钮）。

`removeNode` 是**唯一**会缩短 `nodes` 数组的操作，最容易踩到
「`id === 下标`」这个不变量（`makeNode` 的注释里记着早期被它坑过一次）。
做法很直白：物理移除后把所有 `id` 重排，并同步 `nodeAt` / `grid` 两处反查表。
节点数在几百量级，O(n) 重排完全够用，不值得引入空洞与回收池。
**核心节点不可拆**（拆了整张网就没了），断言守着这一条。

#### 树的观测量：为什么报告里有「平均档位」

树的判据不是「有几棵树」，而是「长到第几档」——
围拢压制的效果只体现在 `treeStageAvg` 上。

树木判据分**两层**，必须分开看，否则会得出错误结论：

- **C 组 · 整局对照**：`⑨ ` 与 `⑩` 用**完全相同**的经营方式
  （优先级/选址/基因/点击频率全一样），唯一变量是「树周围留不留空位」。
  第一版给 ⑨ 配了「共生流」那套（重点找树根），结果它把精力全花在找树上，
  网络规模只有 ⑩ 的 1/10 —— 测出来的差异其实是「穷 vs 富」，不是「树养得好不好」。
  **判据要成立，唯一变量必须只有「树周围留不留空位」。**
- **D 组 · 逐棵受控**：同一局、同一批树里，最挤的那棵保持不动、
  再挑一棵还有空位的围满，一起再长 900 秒，比较两边档位。
  这是「围拢压制」的**因果证据**；C 组拉不开差距时，D 组能区分
  「机制没落地」和「整局经营盖住了机制」。

「克制」的模拟实现也踩过一次：第一版是「把树 2 格内的非树节点全拔掉」，
结果网络被撕得只剩 15 格（对照组 756 格），生产直接崩 ——
那不是「克制」而是自废武功，模拟出来的差异是假的。
真正的克制是**只把树周围的邻居数压到停滞线以下**，
多出来的格子照样铺（那是免费产量）——
「克制」不等于「清空」，这个区别决定模拟结果是不是真的。

### 壤土距离递减：只在普通壤土上做减法（削弱通铺流）

**需求来自玩家方（原文）**：
「我觉得策略度不够，应该改成土壤超过 5 格远以后就是负收益」→
「你要设计的是收益递减的规则，**只动土壤的收益**，这样就可以减弱通铺流的水分，
后续如果要增加孢子就相当于用水分换孢子，这是个好设计」→
「**我想改的只有普通壤土这个地块**」→「都是从水池里扣」→
「第五格变成 0，第六个以后就是负数」→
「**直接用线性做不就行，0.12/0.09**，你算一下 20 格刚好是 -0.45，40 个是 -1.05」。

#### 为什么必须是「加绝对量」而不是「乘折扣」

需求是让产出**归零并转负**。乘法永远做不到归零/转负（`×0.3` 永远是正数），
只有减法能。所以 `TOPSOIL_FALLOFF` 提供的是一个**加减量**，
在 `cellYieldInto` 之后、等级倍率之前加到 `_raw.water` 上。

#### ⚠ 最大的坑：口径没对齐，白绕了一大圈

用户说的「第五格」是**从核心开始数第几格，核心自己算第 1 格**；
我按**距离 dist** 理解（核心 = 0 格），于是把「第 5 格」读成 `dist = 5`，
还为此推导了 `base = 0.5/3`、`zeroAt = 5`、把 `SOILS.soil.yield.water` 清零
一整串东西 —— 全部基于一个错误的坐标。

**统一口径后**（`dist = 从核数第几格 - 1`）：

| 从核数第几格 | 1 | 2 | 3 | 4 | **5** | 6 | 10 | 21 | 41+ |
|---|---|---|---|---|---|---|---|---|---|
| `dist` | 0 | 1 | 2 | 3 | **4** | 5 | 9 | 20 | 40 |
| 产水/s | 0.12 | 0.09 | 0.06 | 0.03 | **0** | −0.03 | −0.15 | −0.48 | −1.05 |

**教训：玩家用「第几格」说话，代码用「距离」说话。先对齐口径，再动数值。**
一旦对齐，整个实现就退化成一条直线，用户给的 `0.12/0.09` 直接就是答案。

#### 最终实现（就是一条直线）

```
offset(dist) = -slope × dist
壤土最终产水 = SOILS.soil.yield.water + offset
             = 0.12 - 0.03 × dist
base = 0.12（= 壤土原本的产出，不动）
slope = 0.03（= 0.12 - 0.09，用户原式）
归零点 = base / slope = 0.12 / 0.03 = 4 格 = 从核数第 5 格
```

**`SOILS.soil.yield.water` 保持 0.12 不变** —— 上一版把它清零是错的口径
带来的多余改动。贴核壤土的产出和以前完全一样，这个改动只体现为「远处变差」。

#### 三个锚点的自洽性

- **第 5 格归零** ⇒ 锁死 `slope = 0.12/4 = 0.03`
- **20 格 = −0.48**（用户说 −0.45，差 0.03，肉眼不可见）✓
- **40 格 = −1.08**，用 **地板 −1.05** 夹住 → 用户要的 −1.05 ✓

⚠ 「第 5 格归零」与「20 格精确 −0.45」**互斥**：
前者要求 `slope = 0.03`（20 格必然 −0.48），后者要求 `slope = 0.0285`。
按用户给的明确式子 `0.12/0.09` 取 0.03。

#### 另一个坑：地板值要减掉 base

`offset` 是**加在 0.12 之上的减量**，所以「产出 −1.05」对应
`offset = −1.05 − 0.12 = **−1.17**`，不是 −1.05。
取 `limit = 1.17` → 地板在 `dist = 39` 生效，40 格正好 −1.05。
（断言里第一次把 `offset` 直接和 −0.48 比，也是同一个混用错误。）

#### 不变量的守护

`tools/autotest.js` 里有 10 条断言守着这个机制：

| 断言 | 守的是什么 |
|---|---|
| `y(0) === 0.12` | 贴核产出没被动过 |
| `y(1) === 0.09` | **用户原式逐格核对** |
| `y(4) === 0`（严格） | 从核数第 5 格精确归零 |
| `y(5) < 0 && y(4) === 0` | 转负发生在第 6 格 |
| 严格单调递减 | 否则不是「递减」 |
| `y(20) === -0.48` | 用户第二个锚点 |
| `y(40) === -1.05` | 用户第三个锚点（含地板） |
| 基础产出仍是 0.12 | 防有人又把它清零 |
| 地板生效 `offset(80) === -limit` | 大图边缘不会一帧吸干水池 |
| 地板触发点 = `limit/slope` = 39 | 防「忘了减 base」的错误 |
| 其它 6 种基质 offset 全为 0 | 用户强调的「**只有**普通壤土」 |
| **面板有 `#topsoilRow` 且显示负值** | **机制必须「看得见」（见下节）** |
| **汇总值 = 逐格累加** | 面板数字与真实扣水量一致 |

#### ⚠ 最贵的坑：机制做对了，但玩家看不见 → 等于没做

改完全部断言都绿了，用户回了一句：

> **「为啥不显示实际产量，我还以为没生效呢」**

查下去发现**代码全是对的，错的是「呈现层」**：

| 玩家可见的位置 | 改前显示 | 问题 |
|---|---|---|
| 左侧水分行的速率 | `rate.water − maintainCost` | ✅ 已含倒扣（`rate.water` 就是 `accepted`） |
| **悬停任意格子的提示** | `soil.yield.water` → **0.12** | ❌ 直接读配置基准，**不含 offset** |
| **发现新基质的日志** | `s.yield.water` → **0.12** | ❌ 同上 |
| **面板有无「壤土在扣水」的行** | **没有** | ❌ 只能靠玩家自己发现水涨得慢 |

三处都写死在 `SOILS[soil].yield` 上 —— 那是**配置基准值**，
而 `computeFlow` 用的是 `cellYieldInto() + topsoilOffset()`。
于是「引擎里在扣水、UI 上显示 0.12」，玩家判定为「功能没做」。

**修法**（三个面都补齐，缺一个都会让玩家怀疑）：

1. **面板**：水行下面加一行 `#topsoilRow`，
   由 `computeFlow` 每帧汇总的 `state.topsoilOffset / state.topsoilCells` 驱动，
   写「壤土递减 12 格　-2.34/s」。**这是机制生效最直接的证据。**
2. **悬停提示**：改成「该距离下的实际产出 + 基准 + 递减量」三段式，
   例如「水 -0.15/s（基准 0.12　壤土递减 -0.27　离核 10 格）」。
3. **发现日志** `describe()`：壤土那一栏直接写清规则
   ——「水 0.12/s（近核，离核越远越少：第 5 格归零、第 41 格起 -1.05/s）」。

**可复用的判据**：任何「引擎里算得对但只在面板上体现为最终值」的机制，
都要单独检查**每一个显示产量的入口**有没有走同一套计算。
配置值（`SOILS[x].yield`）永远只是**基准**，不是**产出**。

**副产品**：`computeFlow` 现在多输出两个字段（`state.topsoilOffset`、
`state.topsoilCells`），面板直接读；自测也顺手加了 6 条断言把这个显示路径钉住。

#### 紧接着的第二个坑：速率符号写死了 `+`

上面刚修完，用户又指出「屏幕右边的产量这栏是不是不太对」。查 `src/ui.js`：

```js
function rate(n) { return '+' + fmt(n) + '/s'; }   // ← 罪魁
```

**前缀 `+` 是硬编码的**。而面板上三种速率**都可能为负**：

| 速率 | 什么时候为负 |
|---|---|
| 水分 | `netWater = rate.water − maintainCost`，维持费超过产出时（后期常态） |
| 水分 | 网络大面积铺远壤土，`rate.water` 本身就被倒扣成负的 |
| 养分 / 孢子 | 运输损耗极端时 |

于是玩家会看到 **`+-1.20/s`** —— 自相矛盾，读起来像 bug（它确实是）。
而且 `fmt()` 用 `toFixed(2)`，`±0.001` 会显示成 `±0.00`：
`+0.00/s` 看着像「有产出」，`-0.00/s` 更是莫名其妙。

**修法**：符号跟着数值走，并把「四舍五入后就是 0.00」的值一律按零处理。

```js
function rate(n) {
  n = Number(n) || 0;
  if (Math.abs(n) < 0.005) return '0.00/s';      // 与 toFixed(2) 的舍入阈值对齐
  return (n > 0 ? '+' : '') + fmt(n) + '/s';
}
```

阈值取 **0.005** 而不是一个拍脑袋的小数，是因为 `toFixed(2)` 的进位点正好在
0.005 —— 这样「显示成 0.00 的值」与「输出裸 `0.00/s` 的值」是**同一个集合**，
边界不会出现一半 `+0.00` 一半 `0.00` 的裂缝。自测把 0.005 / -0.005 两侧都钉住了。

**可复用的判据（和上一条同源）**：**凡是格式化数字的函数，符号都必须跟着数值走。**
`return '+' + ...` 这种写法只在「保证非负」时成立；一旦上游可能出现负数，
它就会产出 `+-` 这种畸形串。本次 `rate()` 的上游恰恰是个减法。

#### 第三个坑：常驻的「尚未触发」是纯噪音

上一版把 `#topsoilRow` 写成**永远显示**，没有格子被扣水时提示
「近核区，尚未触发（第 5 格起归零）」。实测看开局截图才发现问题：
新玩家开局只有 1 格，离归零线还远，这一行**解释不了任何正在发生的事**，
却占着一整行，还让人以为「有个机制我没用上」。

改成**只有真的扣了水才显示**（`nc === 0` 就 `hide` + 清空文本）。
一旦有格子越过归零线，这行立刻带着负数出现 —— 该看见时一定看得见，
没事的时候不占位置。自测加了正反两条断言（有扣水必须可见 / 没扣水必须收起）。

#### 第四个坑（最值得记的一个）：数值全对，但**排版把读者带反了**

反馈原文：**「这个逻辑是不是写错了，咋显示是变成往上加啊」**。

先把数值查了一遍：`offset` 逐格严格递减、面板是净值、`computeFlow` 用的就是同一个
`topsoilOffset` —— **没有任何一处真的在「往上加」**。所以这不是算错，是**读法**问题。
把当时的提示文案抄出来看，问题一目了然：

```
水 0.06/s　（基准 0.12　壤土递减 -0.06　离核 2 格）
```

三个毛病叠在一起，玩家从左边开始扫就会读歪：

1. **结论和过程混在一行**。眼睛先落到 `0.06`，紧接着括号里是 `0.12` 和 `-0.06`；
   两个数并列又没算式，容易被当成「0.06 是加出来的」。玩家看不见 `0.12 − 0.15` 这一步。
2. **`离核 2 格` 用的是 0 基的 `dist`**，而玩家心里数的是「从核第几格」。
   他悬停自己认为的「第 3 格」，文案写「离核 2 格」—— 第一反应是「位置对不上，肯定写错了」。
   而这个口径我们**已经为他改过一次**（见上文 0.12/0.09 那段），却在提示文案里漏了。
3. **`壤土递减` 带了条件式前缀 `(off > 0 ? '+' : '')`**。它是死代码（`off` 恒 ≤ 0），
   但留着就是在给下一个人（和 reviewer 的眼睛）提供「这里会加」的心理暗示。

**改法 —— 两条硬规则，写进代码注释防回退：**

- **结论第一行、单独一行**：`水 -0.03/s`。负号直接顶头，不加任何前缀加号。
- **过程第二行，写成算式**：`0.12 − 0.15 = -0.03（从核第 6 格）`。
  算式里的减号**不可约**——只要它是算式，就不存在读成加法的路径。
- 距离一律换算成**玩家的「从核第 N 格」（= `dist + 1`）**，`dist` 只留在代码内部。

改完的读数是一条干净的滑坡，一眼就能看出方向：

| 悬停 | 提示第一行 | 提示第二行 |
|---|---|---|
| 从核第 2 格 | `水 0.09/s` | `0.12 − 0.03 = 0.09` |
| 从核第 4 格 | `水 0.03/s` | `0.12 − 0.09 = 0.03` |
| **从核第 5 格** | `水 0.00/s` | `0.12 − 0.12 = 0.00` ← 归零 |
| **从核第 6 格** | `水 -0.03/s` | `0.12 − 0.15 = -0.03` ← 转负 |
| 从核第 7 格 | `水 -0.06/s` | `0.12 − 0.18 = -0.06` |

面板汇总行同步改成 `壤土递减 12 格　-2.34/s（越远扣得越多）` ——
**整行不允许出现任何 `+`**，并把「越远扣得越多」直接写在括号里，方向不靠推断。

**可复用的判据（比前面三条更抽象，也更重要）：**

> **当一个数可能为负时，不要只保证「符号正确」，还要保证「读法唯一」。**
> 把结论和推导分成两行、推导写成算式、把内部口径（0 基 `dist`）换算成用户口径
> （从核第 N 格）—— 这三件事让「读反」在结构上变得不可能，
> 而不是指望玩家读得足够仔细。

配套：`autotest.js` 新增 **10 条字符串断言**（负产出不带加号、必须是减法算式、
距离必须是玩家口径、归零格不写负号、面板行不含 `+`…），
刻意只断言**字符串**不碰 UI —— 目的是以后谁改文案，谁就得先过这一关。

#### 副作用

贴核 4 格不变（0.12/0.09/0.06/0.03），第 5 格起转负 ——
**比上一版温和得多**（上一版从第 1 格就开始涨到 0.167、5 格外骤降）。
开局水量压力基本没有变化，收水只发生在「铺远」这件事上，
正是设计想要的：**只惩罚通铺，不惩罚正常玩法**。

### 自测跑的是「上一版代码」——`src/*.js` 没有版本号

`tools/run_autotest.py` 起本地 http 服务 + Chrome headless 抓报告。
有个隐蔽陷阱：**`index.html` 用 `<script src="src/config.js">` 引脚本，
没有 query 版本号**，Chrome 会把它们缓存进 `tools/_chromeprofile_<port>/`。
于是改完 `config.js` 再跑自测，**跑的可能是上一版** ——
表现为断言值对不上（曾看到过期的 `offset(20) = -0.6` 而源码明明是 -0.48）。

**三重保险**：
1. 启动参数加 `--incognito --disable-application-cache --disk-cache-size=1`
2. 换端口就换 profile（脚本已按 `PORT` 命名 profile 目录），必要时直接删掉
3. **交叉验证**：自测报的数值要和 `node -e "require('./src/config.js')"` 的结果对得上。
   两边不一致时，先怀疑自测跑的是旧代码，别改源码。

### 报数前先问「这个数是 offset 还是最终产出」

**混用 offset 与最终产出，会让正确的代码看起来是坏的。**
壤土的最终产出 = `SOILS.soil.yield.water` + `topsoilOffset(...)`，
两个数差一个 base（0.12）。实例：`offset(20) = -0.60`，
但**最终产出** `= 0.12 - 0.60 = -0.48`。

第一次做断言时，我拿 `offset(20)` 去比 `-0.48` → 断言 FAIL，
而**代码其实完全正确**。凡是对外报给玩家的数，一律用 `yAt()`（含 base）；
只有测「机制本身」时才看裸 offset。

---

### 平衡模拟跑得久，别用前台 timeout

1800 秒 × 5 种子 × 6 策略 + B 组对照，实测 **2 分 15 秒**。
前台跑会被 2 分钟默认 timeout 打断，而截断位置恰好落在 B 组标题之后 ——
看起来像「跑到 B 组就崩了」，实际上是超时。**长跑一律用 `run_in_background`。**
（加入树木后策略扩到 10 条、再加 D 组 20 局受控实验，实测约 **3 分钟**，
更容易撞线。）

### 调平衡前先问「这个参数在回答什么问题」

这一整节最贵的一课。数值参数其实分两类，混起来就会把设计问题当平衡题去调：

- **平衡旋钮**（`crowdPenalty`、升级成本增速、维持费系数）——
  该按**实测承载力**反推，调错了改数值就行；
- **设计承诺**（「树要能影响周围」「古树 = 养出来的东西」）——
  只该满足**语义要求**，不该按「养不养得起」去改。

`crowdSoftCap` 属于第二类。我前四轮都在按「怎样才平衡」调它，
真正的判据却是「玩家正常玩时树能不能长起来」——
那是**实测分布**回答的问题，不是平衡感回答的问题。
**先问「这个参数在回答什么问题」，再算数值。**

---

## 存档系统：自动存档和槽位必须分家（2026-09-21）

**需求**：「加一个开始新游戏和读取存档的功能」。

上手发现上一版**已经有** 保存 / 读取 / 重开一局 三个按钮，
但它们其实是坏的 —— 问题在存储层而不是 UI 层：

```js
var KEY = 'mycelium_save_v1';        // 自动存档和「读取」共用这一个 key
```

自动存档在**每次保存、每次关页面**都覆盖 `KEY`，
而「读取」读的也是 `KEY`。于是点「读取」读到的就是**刚刚被覆盖的当前局** ——
功能形式上存在、实质上等于没有。而且它还会**静默替换你的这一局**（无确认）。

### 改法：两层存储，职责分开

| 层 | key | 谁写 | 谁会覆盖它 |
|---|---|---|---|
| **自动存档** | `UI.KEY` | 保存按钮 / 关页面 / 换局 | 自动流程，**永远代表当前这一局** |
| **槽位** | `UI.slotKey(n)`，n=1..3 | 玩家手动「存入」 | 只有玩家点「覆盖」 |

命名空间特意分开（`mycelium_slot_1_v1` vs `mycelium_save_v1`），不只是加后缀 ——
以后加槽位数量或迁移格式时，不会误伤自动存档。

### 摘要存进同一个 JSON，而不是另存一份

槽位列表要显示「菌丝 320 格 / 300:00 / 5 小时前 / 转生 3」才能让玩家认出该读哪个。
有两种存法：

- ❌ 另存一份 summary 到别的 key —— 两份数据会不同步（典型的双写问题）
- ✅ 往 `Sim.serialize()` 的结果里塞一个 `_meta` 键

选后者。`deserialize()` 只读它认识的字段、忽略未知键，
所以 **`sim.js` 一行都不用改**，而且摘要和正文天然一致。

### 「开始新游戏」和「重开一局」是两件事

上一版把这些混在一个「重开一局」按钮里，名字还和「读取」很像，玩家分不清。
现在：

- **开始新游戏** → `Sim.newGame(seed, {}, {})`，**基因/知识/转生全清**（有 confirm）
- 保留基因只换地图 → 不再单独占按钮，收进 `newGame(keepGenes)` 的参数

### 读档后必须同步自动存档

```js
UI.rebind(s);
resetSceneCache();      // scene 里一堆上一局的缓存要清
this.save(true);        // ← 关键
```

漏掉 `save(true)` 的话：读档后**刷新页面会回到读档前的那一局**。
这种 bug 极难自查（你要先读档、再刷新、才发现前功尽弃），
所以自测里专门有一条断言钉它。

顺带把「清场景缓存」抽成 `resetSceneCache()` ——
上一版这个四行清理在 `load` 和 `newSeed` 里各抄了一遍，
加第三个入口时一定会漏改一处。

### 自测的两个坑（都踩了）

1. **`addNode` 之后必须 `invalidateCands`**：`candidates()` 按帧缓存，
   循环里连加节点不清缓存，拿到的永远是同一批旧候选（网络长不大）。
2. **测完读档必须把当前局复位**：为了验证「读档真的换了状态」，
   测试会把 `g.state` 换成自己造的存档。如果不复位，
   后面所有依赖「网络会自己长起来」的断言全跑在半成品上 ——
   实测表现为 `maxDist=0`、养分不产出，看起来像 10 个回归，
   实际只是测试污染。**造脏数据的测试，负责把现场打扫干净。**

---

## 菌株系统：为什么乘算而不是加算（2026-09-21）

菌株换的是**规则**，不是数值。四个菌株各改一组乘法系数，全部走
`strainMods(state)` 集中出参，缓存在 `state._strainMods` 上由
`invalidateStrain(state)` 失效 —— **只要改了 `state.strains` 就必须调它**，
否则拿到的是上一套的缓存（症状：换菌株后面板数字纹丝不动）。

### 叠加必须相乘

两个菌株的加成落在同一格时**相乘**：

```js
soilMul[soil] = a.soilMul[soil] * b.soilMul[soil];   // 不是 +
```

这是刻意的。加算下「带两个菌株 ≈ 两个 Buff 相加」，
最优解永远是**把最强的那两个装上**，选择退化成排序题。
乘算才会出现「叠加互相拆台」：

| 组合 | 结果 |
|---|---|
| 速生 + 运输 | 铺得便宜 × 远距离不掉损耗 → **真共振** |
| 速生 + 腐生 | 速生的 `distLossMul:1.35` 吃掉腐生的远距离收益 → **几乎等于只带腐生** |

第二条是实测出来的（`_combo.js` 打点：两株都装上了，`EQOK rapid@t=110 | EQOK saprophyte@t=282`，
但总分与单带腐生**一致**）。这不是 bug，而是这一步的设计成果 ——
**存在要动脑的搭配，而不是无脑全上。**

### 槽位：`strainSlots` 读的是里程碑，不是当前网络

```js
n = START.strainSlots (1)
n += state.milestones.m11 ? 1 : 0
n += state.genes.gStrain || 0        // 预留
n = Math.min(STRAIN.maxSlots, n)     // 2
```

`equipStrain` 在槽位满时**拒绝**（返回 `{ok:false, reason:'槽位已满（N 个），先卸掉一个'}`），
不做自动顶替 —— 自动顶替会让玩家不知道自己现在带着什么。

### `strainsUnlocked` 必须同时看当前格数和历史最高

```js
return state.nodes.length >= cfg.unlockNodes ||
       (state.counters && (state.counters.maxNodes || 0) >= cfg.unlockNodes);
```

解锁是 **40 格**，门槛很低，但**转生会把网络清到 1 格** ——
只看 `state.nodes.length` 的话，转生后菌株面板会突然「锁上」，
玩家刚配好的 Build 消失。所以像其他所有「历史成就」一样走 `counters`。

---

## 离线收益：口径比参数重要（2026-09-21）

### 为什么不做真模拟

规划文档说「离线期间计算基础生产和自动扩张」。第一版思路是
`for (i < awaySec/DT) tick()`，但 **8h / 0.25s = 11.5 万次 tick**，
再叠加 `upgradeNodes` 的内层循环，直接把进程跑到 OOM 被系统杀掉
（症状极像「代码挂了」：**没有任何输出、exit 1、stdout 空**）。

改成**折算 + 线性外推**：`settleOffline` 不 tick，只调
`computeFlow(state, 0)` 拿一份稳态速率，然后

```js
gained = rate × min(awaySec, capHours×3600) × offlineEff
```

代价是丢掉「离线期间网络会自己长」这层 —— 但 `allowAutoGrow: false`
本来就不打算给（给了会让「关掉等」变成最优解）。**口径反而更可控。**

### 早期下限是必须的，不是锦上添花

自动蔓延要里程碑解锁。如果玩家刚开一局就关掉页面，
`computeFlow` 给的速率接近 0 → 回来发现**什么都没发生**，
体验是负的。所以给水单独一条兜底：

```js
var floorWater = coreSeep(state) * (cfg.coreFloorMul || 1);
water: Math.max(rate.water, floorWater) * useSec * eff
```

`coreSeep()` 是这一步新抽出来的（`START.seep + genes.gRate × 0.4`），
`tick` 里的 `state.seepRate` 也改走它 —— **同一份公式只许有一处**。

### 验证口径：F 组必须「同快照、同秒数」

`headless_sim` 的 F 组**不能**拿「离线 8h」和「在线跑了 8h 的累计量」直接比，
因为在线那 8h 里玩家会扩容、会升级，速率一路在涨。
所以两边从**同一个成熟快照**出发（先正常玩 3600s，再 `serialize`/`deserialize` 复制两份），
在线侧先 tick `warm` 秒走完「扩容 → 升级 → 再扩容」的爬升段，
再取一个**稳态窗口**实测产量、乘系数外推 ——

```js
const win  = windowSec || 600;
const warm = Math.min(win * 4, 1200);   // ← 必须有硬上限
```

**`warm` 的硬上限是踩出来的**：曾经写成 `warm = win * 4` 而窗口给到 1800s，
于是要 tick 9000s × 5 条对照组 → F 组直接卡死。

判据（`offlineEff` 是否合格）：

| 在线/离线 比值 | 结论 |
|---|---|
| < 1.5 | ❌ 最优玩法变成「关掉等」 |
| 1.5 ~ 2.5 | ⚠️ 勉强合格 |
| > 2.5 | ✅ 动手玩明显更划算 |

实测 8h 口径：**在线/离线 = 养分 ×8.47　孢子 ×11.06**（离线只有在线的 12%）。

### 快照里要手动补 m11

F 组用的是 `run()` 出来的成熟局面，但 `run()` 里 m11 是模拟玩家点出来的。
`deserialize` 之后如果不补 `snapshot.milestones.m11 = true`，
在线侧和离线侧的**槽位数不一致**，比出来的差异是假的。补完要
`Sim.invalidateStrain(snapshot)` 让缓存失效。

---

## 转生三选一：为什么地图不再自动变大（2026-09-22）

### 需求

> 每次转生希望做成 roguelike 那种感觉，每次转生可以选择获得一种菌种
> 或者扩大地图，或者得到更多的转生的点数。

关键决定（和用户确认过的三条）：

1. **「菌种」就是现有菌株** —— 复用已有的 4 种，不新开一层稀有度
2. **每次转生必给三选一** —— 保证节奏稳定，而不是偶尔才弹
3. **「扩大地图」改成选项：选了才变大** —— 这是这次改动的核心

### 为什么必须把地图从「自动奖励」改成「选项」

改之前：`mapSizeFor(prestiges)`，转生一次地图自动变大一次。

这看起来是福利，实际上是**把抉择偷走了**：地图白拿，玩家不需要为它
放弃任何东西，于是「要不要更大的地图」这个问题根本没有被问出口。
三选一里塞一张「免费就有的卡」，那张卡永远是废牌。

改之后：`mapSizeFor(mapTier)`，`mapTier` 只在选了地图卡时才 +1。
想要更大的地图，就得放弃那一轮的菌株和基因点。**白拿的东西不算选择。**

代价是要做存档迁移（`mapTier` 是新字段）—— 见上面「转生换图」。

### 抽卡算法：先保席位，再按权重补满

第一版是「把三类卡丢进一个袋子按权重抽 3 张」。实测（`tools/_drawstat.js`，
N=4000）**地图卡在开局只有 36% 的出现率**：4 张菌株卡权重各 3（共 12）
把单张地图卡权重 2 挤掉了。

这是个很糟的体验：**存在但摸不着，比没有更让人恼火** —— 玩家会以为
「扩大地图」是个没做完的功能。

改成**按类别占席**：

1. 每个「还有货」的类别（菌株 / 地图 / 基因点）**先各占一个席位**
2. 剩下的席位再从展平的袋子里按权重补满，已抽中的 key 不重复

于是「只要还有货，每一类都一定出现」= 100%（实测）。权重只用来决定
**同一类别内部**抽到哪一个（比如 4 种菌株里抽哪种）。

### 三个必须钉住的不变量

三选一是**阻塞式**交互，出错的方式比一般功能难受得多，所以每条都有断言：

| 风险 | 后果 | 防线 |
|---|---|---|
| 卡没弹出 | 玩家卡住，什么都点不了 | `pendingChoice` 进存档；UI `rebind` 时同步弹窗 |
| 卡能重复选 | 白拿两份奖励 | `applyChoice` 立刻清空 `pendingChoice`（幂等） |
| 卡丢了 | 奖励蒸发 | `pendingChoice` 随存档往返；坏形状（空 cards）在 `deserialize` 里丢弃 |

`pendingChoice` 的形状校验是必要的：它是**唯一**一个「跨存档的临时状态」，
磁盘上的 JSON 可能来自旧版本或被手改过，直接信它会让玩家点到崩。

### 槽位满了的菌株卡不能吞掉

`applyChoice` 选菌株卡时走 `equipStrain`。槽位满会被拒绝 ——
但这时**不能把卡吃掉**，否则玩家白白浪费一次转生奖励。
做法是：记进 `state.strains`（已拥有，等玩家自己腾位置），
并把拒绝理由写进 `card.note`。

### headless_sim 里必须有人替玩家选卡

`doPrestige` 只是把选择**挂**到 `pendingChoice` 上，奖励要等 `applyChoice`
才落地。模拟里没人选的话：`pendingChoice` 一直挂着，下次转生又把它覆盖掉 ——
**「三选一」在 headless 里完全是个空操作，而且不报任何错**，
跑出来的所有策略数值都等于「玩家从不选卡」。

所以 `run()` 里每次转生后调 `pickChoice(state, strategy.choice)`，
并统计各类卡被选了几次（`choices`），**G 组靠它确认卡真的落地了**。

### G 组判据：三张卡必须互相竞争

固定同一套经营方式（②扩张流），**唯一变量只有「选哪类卡」**：

| 差距 | 判断 |
|---|---|
| < 1.10× | ❌ 选什么都一样，卡片是装饰 |
| 1.10 ~ 2.50× | ✅ 三张卡在同一量级，各有偏好 |
| > 2.50× | ⚠️ 某张碾压其余，实际只有一种选法 |

对照组**必须带 `strains: []`** —— 否则「菌株」那一档会额外吃到 E 组的
菌株加成，比的就成了「有菌株 vs 无菌株」，而不是三张卡本身。

**实测（`headless_sim 1800`，5 种子，A~G 全组无 ❌）**：

| 选哪类卡 | 转生 | 基因点 | 终局菌丝 | 地图档位（尺寸） | 综合分 |
|---|---|---|---|---|---|
| 菌株 | 6.40 | 176.2 | 470 格 | 0（34×20） | **44914** |
| 基因点 | 6.00 | 144.0 | 602 格 | 0（34×20） | 34892 |
| 扩大地图 | 6.00 | 57.2 | **747** 格 | 6（58×34） | 25810 |

最优/最差 = **1.74 倍** → 判据成立：三张卡都在竞争，没有一张永远最优。
三张卡的「赢面」也互不相同：菌株卡滚雪球最快、点数卡给基因最多、
地图卡把网络铺到最大（747 格）却牺牲了永久成长（基因只有 57.2）。

**同一组在 600s 下只有 1.12 倍** —— 这个对照本身就是结论：
「扩大地图」是**长期投资**，短局它最弱（地图刚变大、网络还没铺满），
长局才追上来。所以**引用 G 组数字必须带时长**，和 A/B 组同样的规矩。

值得一提的是长局里那条降级路径真的跑到了：菌株卡被选了 20 次、
点数卡 12 次 —— 4 种菌株全拿到之后菌株池自然空了，
引擎自动退让到点数卡，**没有报错也没有发出重复的菌株**。

**快速复看**：`node tools/probe_choice.js 600` 只跑 G 组。
它按注释锚点把 G 组的代码块**原样**挪到 C/D 之前执行 —— 用的是同一个
`run()` / `avgOf()` / `STRATEGIES`，所以数字和正式报表一致，
不会出现「探针一套、报表另一套」。代价是依赖 headless_sim.js 里的三处锚点，
改过那几行注释会「定位失败」并退出 2（宁可报错，也不给错数字）。

### 顺手修掉的引擎隐患：`id === 数组下标`

排查这次改动时被 9 个自测失败卡了很久，根因不是三选一，而是一个**老隐患**：

引擎通篇按 `state.nodes[id]` 当下标用（`rebuildNetwork`、`computeFlow`
都这样），所以「`node.id` === 数组下标」是个**隐式不变量**。
`removeNode` 会自己重排 id，但自测里有一处**手动 `splice`** 没重排 ——
于是 `state.nodes[id]` 取到 `undefined`，然后在几百行之外的 `computeFlow`
炸成 `Cannot read properties of undefined (reading 'prod')`，
**报错点与真正的原因相隔极远**，一次性带崩 7 个步骤。

两处修正：

1. `rebuildNetwork` 开头加 O(n) 守门：id 与下标错位就压实 + 重排，
   并同步 `nodeAt` / `grid`。它只在结构变化时调用（不是每帧），代价可忽略。
2. 那个自测步骤改用 `Sim.removeNode`（引擎自己的拆除接口），不再手动 `splice`。

顺带把「步骤抛异常」的报错改成**带步骤编号和第一行注释**
（之前一次 9 个同样的异常消息，根本看不出是哪一步炸的）。

---

## 开发环境注意

本项目在 Windows 上开发，踩过的环境坑：

- **Bash 工具的 cwd 不会保持**（会话工作区不在项目目录）——
  后台任务、重定向、`http.server` 的根目录全部要用**绝对路径**，否则静默 404 / 写错位置
- **`http.server` 加 `--directory <项目路径>`** 指定根目录
- **PowerShell 的 stdout 经常不回传到工具结果** —— 输出重定向到文件后再用 Read 读
- **后台任务用 `run_in_background`**，别用 `sleep` 轮询
- shim 环境有时会丢命令（`sleep` / `grep` / `dirname` / `ls` / `cat` 报 command not found）——
  遇到就改用 Python（`time.sleep` + `urllib.request`）完成等待与探测
- **`git add -A` 前先清理临时产物**（见上文）。另外本仓库主分支是 `main`，不是 `master`
- 打包用 Python `zipfile`，`Compress-Archive` 在本机会静默失败（只建空目录、退出码仍是 0）

### 验证线上版本时，**不要**加 `Cache-Control: no-cache`

`tools/verify_online.py` 拉线上文件做断言。踩过的坑：
给请求加上 `Cache-Control: no-cache` / `Pragma: no-cache` 之后，
**GitHub Pages 的 CDN 会返回一层陈旧缓存**（拉到旧版内容），
于是刚推送的新代码全部断言失败 —— 看起来像「推送没生效」，
实际上 `git log origin/main` 早已是新提交、也是新提交。

同一个 URL、同一时刻，另一个只带普通 UA 的脚本拿到的是新版：

| 请求头 | 拿到的 config.js |
|---|---|
| `Cache-Control: no-cache` | 9537 字节（旧） |
| 仅 `User-Agent: Mozilla/5.0` + `?cb=<时间戳>` | 10863 字节（新）✓ |

**正确做法**：只加普通 UA + 一个时间戳 query 参数绕缓存。加时间戳就够了。

### 验证线上，要跑代码而不是只查字符串

`tools/verify_online.py` 只做「字符串是否存在」的断言，抓不出运行时问题。
更强的一档是 `tools/verify_online_run.py`：把线上 `src/` 和 `index.html`
**整个拉下来搭成本地站点**，再对这份副本跑完整的 `?autotest=1` 自测 ——
断言的是**线上实际部署的代码**，而不是本地工作区。

踩过的坑：**别漏 `vendor/phaser.min.js`**（1.2 MB）。漏了它 Phaser 加载失败，
表现是自测只有一条 `FAIL 游戏未能在 8 秒内启动` ——
看起来像线上代码坏了，实际是 staging 目录文件不全（假失败）。

---

## 已知可继续做的方向

- 稀有词条掉落（怪物/腐木掉特殊菌株），给挂机回来时制造惊喜
- 多张地图 / 地形主题（针叶林、沼泽、火山灰土）切换
- 音效与音乐（Phaser 的音频系统还没用上，目前完全是无声的）
- 更细的自动化：多条自动扩张路线、优先级队列
